# Database (SQLite for local PoC; switch to Postgres URL when moving to Supabase)
DATABASE_URL="file:./dev.db"

# Base URL (used in magic links)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Session secret (random string)
SESSION_SECRET="change-me-in-production-use-a-long-random-string"

# Admin bootstrap: comma-separated emails that are auto-admin on first login
ADMIN_EMAILS="infojongsma@gmail.com"
